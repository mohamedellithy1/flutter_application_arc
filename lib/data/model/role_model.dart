class RoleModel {
 String uuid, displayName, description, displayIconp;
 
  RoleModel({
    required this.uuid,
    required this.displayName,
    required this.description,
    required this.displayIconp,
  });
}